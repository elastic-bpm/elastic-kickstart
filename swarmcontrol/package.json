{
  "name": "swarm-control",
  "version": "1.0.0",
  "description": "Controlling the swarms",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "dependencies": {
    "azure-arm-compute": "^1.0.0-preview",
    "ms-rest-azure": "^2.1.1"
  }
}
